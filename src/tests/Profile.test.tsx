import React from 'react';
import { render } from '@testing-library/react';
import Profile from '../pages/Tabs/Profile';

test('page should have ...', async () => {
  // const { findByText } = render(<Profile />);
  // await findByText('Score');
});